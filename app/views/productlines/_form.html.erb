<%= form_with(model: productline, local: true) do |form| %>
  <% if productline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(productline.errors.count, "error") %> prohibited this productline from being saved:</h2>

      <ul>
      <% productline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :textDescription %>
    <%= form.text_field :textDescription, id: :productline_textDescription %>
  </div>

  <div class="field">
    <%= form.label :htmlDescription %>
    <%= form.text_area :htmlDescription, id: :productline_htmlDescription %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :productline_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
